# 协作与设计偏好

本文件记录工作区长期偏好；用户当前明确要求优先。执行、构建、安全和发布规则见适用的 `AGENTS.md`。

## 方向与执行

- 产品、交互或架构方向未定，或取舍会显著影响体验、维护成本、兼容性时，先收集事实，再针对关键分歧提问并说明推荐理由；不要求每次覆盖完整需求清单。
- 方向明确后自主完成实现与必要验证，不反复确认。明确的修复、沿用现有设计和常规实现细节直接处理。
- 默认由主代理修改代码，subagent 只做只读分析或独立审计；只有用户明确要求使用多代理修改时，才委派 subagent 修改代码。复杂任务先制定可执行计划，简单任务不增加多模型流程。
- 界面与交互改动贴合 BongoCat 现有风格：透明无边框窗口、置顶桌宠表现，不额外引入视觉噪音或多余设置项。

## 独立审计与沟通

- 每完成一次代码修改（含修复、重构、文案与界面调整）都要做独立审计，只读检查最终差异，覆盖功能正确性和用户体验；发现可操作问题后修复并复审，结论明确为 CLEAN 或列出问题。
- subagent（包括独立审计）默认使用模型 ID `gpt-6-astra`、`medium` 推理强度；用户在当前对话中指定了模型或推理强度时，一律以用户指定为准（含临时指定某个模型跑审计）。模型不可用时说明情况，不自行改用其他模型。
- 每个新功能新开独立审计代理，不复用其他功能的审计代理；同一功能审计后修改及复审，复用该功能的审计代理。
- 交互审计检查窗口透明度、置顶与穿透、拖拽、托盘与偏好页切换、模型加载与切换是否连贯；区分静态审计、本地运行与打包后验证。窗口置顶/穿透/托盘这类只能靠运行观察的现象，审计只做静态判断，实际表现由主代理运行验证。
- 独立审计只做只读代码审计：不启动应用、不安装或打包、不操作桌面；本地运行与打包验证由主代理单独进行，审计可以只读地查看日志或产物作为证据。
- 通过消息或工作区进展了解 subagent 状态，平和询问卡点，不催促。空消息或简短 keepalive 不等于卡死；仅在长时间没有有效进展时排查。
- 新的稳定偏好记入本文件，一次性要求不沉淀。上游协作收尾按仓库 `AGENTS.md` 执行。

## 沟通

- 用简洁易懂的中文交流，先说结论，再补必要的细节；避免长段落和大段术语堆叠。

## 提交与推送

- 提交（commit）、推送（push）、创建或更新 PR、打标签或发布等对外动作，一律先取得用户明确同意再执行；未明确要求时只做到本地改动、验证与报告。
