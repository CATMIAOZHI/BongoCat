# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.pnpm-store
dist
dist-ssr
*.local
target

# 凭据绝不入库（例如 server-relay 的 .env、generate-pair 写出的 pair-secret.txt）；
# 子目录各自的 .gitignore 也有一份，这里是兜底，避免在仓库根误写文件后带进提交。
.env
pair-secret.txt

# Editor directories and files
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
