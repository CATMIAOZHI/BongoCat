# 复制成 .env 再填（.env 已在 .gitignore 里忽略，不要提交）。
#
# Pair Secret：双方客户端填同一个值，中继自己从它派生 PAIR_AUTH_TOKEN。
# 用 `cargo run --release --bin generate-pair -- --write` 生成。
PAIR_SECRET=

# 客户端要填的域名，也是 Caddy 申请证书用的域名。
# 必须是域名：客户端只认内置的 Mozilla 根证书库（不看系统证书存储），
# 裸 IP 与自签证书连不上。
PAIR_DOMAIN=cat.example.com
